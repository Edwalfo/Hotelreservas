const modelCategoria = document.getElementById('model-Categoria');
const modelHuesped = document.getElementById('model-addHuesped');
const modelEstadoReservas = document.getElementById('model-estadoReserva');
const modeltipoDocumento = document.getElementById('model-tipoDocumento');
const modelestadoHabitacion = document.getElementById('model-estadoHabitacion');
const modelAddHabitacion = document.getElementById('model-addHabitacion');
const modelAddUsuario = document.getElementById('model-addUsuario');

/*
modelAddUsuario.addEventListener('show.bs.modal', function (event) {
  // Button that triggered the modal
  let button = event.relatedTarget;
  // Extract info from data-bs-* attributes
  let recipient = button.getAttribute('data-bs-whatever');

  // Use above variables to manipulate the DOM
});*/


/*

const cardhabitaciones = document.querySelectorAll(".habitaciones");


function accionarCard(event) {
  let valor = event.currentTarget.value;
  

  console.log(valor)

}

cardhabitaciones?.forEach(id => {
  id.addEventListener("click", accionarCard);

});*/












